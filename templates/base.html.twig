<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>

<header class="site-header" style="background-image: url({{ asset('img/background.jpg') }})">
    </header>
            <nav>
        <a href="{{ path('home') }}">Home</a>
        <a href="{{ path('about') }}">About</a>
        <a href="{{ path('report') }}">Report</a>
        <a href="{{ path('pig_start') }}">Pig game</a>
        <a href="{{ path('card_start') }}">Blackjack</a>
        <a href="{{ path('lucky_number') }}">Lucky number</a>
        <a href="{{ path('api') }}">API</a>
        <a href="{{ path('session_page') }}">Session</a>

</nav>
    <body>
<main>
        {% block body %}{% endblock %}
        </main>

    </body>   
<footer>
    <p>Denna sidan är copyright &copy; av mig.</p>
</footer>
</html>
